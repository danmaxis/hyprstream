node_modules/
dist/
# built plugin bundles are artifacts — distribution is via the packaged
# .streamDeckPlugin on GitHub Releases, produced by `npm run package`.
*.sdPlugin/bin/
*.log
.DS_Store
*.streamDeckPlugin
.cache/
coverage/

# agent / sandbox scratch — never commit
.gi/
.sandbox-home/
.sandbox-tmp/
